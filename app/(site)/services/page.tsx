import { Section } from "@/components/ui/Section";

export const metadata = { title: "Services" };

export default function Page() {
  return (
    <Section eyebrow="Services" title="Services">
      <p className="max-w-2xl font-body text-white/60">
        Content pending — see /content-required in the foundation document.
        This route is fully wired and ready for CMS content.
      </p>
    </Section>
  );
}
